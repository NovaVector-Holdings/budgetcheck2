# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# This repo has never committed a lockfile; keep that intentional rather
# than letting one get tracked by accident from a future `npm install`.
package-lock.json

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Local environment config -- see .env.example for the (non-secret) template
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
